# Production Environment
NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1

# Database Configuration
DB_ENVIRONMENT=production

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=your-super-secure-production-access-secret-key-32-chars-minimum
JWT_REFRESH_SECRET=your-super-secure-production-refresh-secret-key-32-chars-minimum

# CORS (Allow all origins)
CORS_ORIGIN=*